# Godot 4+ import cache and editor state. Never commit this.
.godot/

# Export output
/export/
/build/
/android/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

*.swp
*~

# Throwaway verification harnesses
tools/_scratch/

# Godot writes this at the root with local absolute paths the moment the
# export dialog is opened. CI generates its own from tools/ci/.
export_presets.cfg
