{
  "base_rules": "res://resources/rules/default_match_rules.tres",
  "notes": "The guard's shot as a travelling round: hitscan, a slow bullet a runner can beat by breaking stride, and a fast one that mostly still punishes being seen.",
  "variants": [
    {"name": "hitscan", "notes": "0 m/s = the shipped game, the shot and the trigger are one event", "overrides": {"guard_projectile_speed": 0.0}},
    {"name": "mps_150", "notes": "0.67 s of flight at 100 m; ~4.7 m of lead on a sprinter", "overrides": {"guard_projectile_speed": 150.0}},
    {"name": "mps_300", "notes": "0.33 s of flight at 100 m; ~2.3 m of lead on a sprinter", "overrides": {"guard_projectile_speed": 300.0}}
  ]
}
