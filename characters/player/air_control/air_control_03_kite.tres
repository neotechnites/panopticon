[gd_resource type="Resource" script_class="AirControlPreset" load_steps=3 format=3]

[ext_resource type="Script" path="res://scripts/player/air_control_preset.gd" id="1_preset"]
[ext_resource type="Resource" path="res://scenes/player/default_movement_profile.tres" id="2_base"]

[resource]
script = ExtResource("1_preset")
base = ExtResource("2_base")
id = &"kite"
display_name = "Kite"
feel = "You go where you point, in the air exactly as on the ground, because the air now drags almost as hard as the floor does -- the strafe still works but it barely pays, so speed has to come from the slide and from the run-up rather than from the mouse."
max_air_speed = 11.0
air_acceleration = 16.0
air_friction = 5.0
auto_bunny_hop = true
