# Map 3: the Forest

A clearing under a closed canopy. A foggy ravine round a great tree; the tree is the tower, grown for the purpose.
World coordinates, Godot y up; bearings as map 1 (`pol(bearing, r, z)`). Second pass (Ryan on the first: "a very very good first pass").

1. Lane: grass annulus r 46.7..57.3 at y 23.0, gentle 2-D relief +-0.12 m (zero at both edges), painted as fine noise in three gold-olive greens, a worn brown-green path r 50.5..53.5 with a verge one row either side, lane r 52, route 5 deg to 335 deg; a fallen log with a stick fence across the lane at 350 deg (collider box to y 34.5). Unchanged from pass 1 except the fixtures: the bars at 353 deg and the portal at the finish are now THIS map's own, not Map 1's hell rock (Ryan: "now create bars and portals that match the actual maps"). `forest_bars.glb` is the map's own cell bars and fallen-log fence scaled into a gate -- a log sill, two living end posts, a lintel, 20 BRANCHES socketed into the sill's top quads and the lintel's underside, two lashing rails, leaf sprays -- in rock_bars.glb's exact envelope (10.6 x 8.5 m, 0.79 m thick, origin the base centre) and keeping its guarantee that no gap exceeds 0.38 m (widest 0.338 m, bar-bar at z 4.27; the edge slot 0.094 m). Second pass on the bars (Ryan: "the forest bars should look like natural branches, not just bars made of wood. and i mean that in shape not just through some leaves on it"): a bar is no longer a bowed dowel but a branch -- an irregular taper that sheds most of its girth in its first third, its own bow through the thickness, a wander and one or two kinks across the lane, a swelling at each knot, short cut stubs where side branches were taken off, and on every fourth bar a fork near the top whose second limb welds into the lintel's FRONT face (so a fork costs no extra lintel geometry). The lintel's lane boundaries drift across the gate, so a bar's head does not stand over its foot and the screen is a fan, not a comb. Every number comes off the one seed. `forest_portal.glb` is two trunks braided over the opening into a pointed arch with branches and leaf clumps over it, round portal.glb's exact opening (4.5 x 4.0 x 0.8 m envelope, 2.7 m clear at the ground, springing 2.60, apex 3.15, surface centre 1.50, clear gate 2.60 x 2.80), so scenes/ring/forest_portal.tscn is portal.tscn node for node -- same Glow, same Gate, same finish_gate group -- and the effect surface keeps portal_build's own swirl painter verbatim and the same doubleSided emissive material, painted into the forest atlas's spare `earth` cell. 2806 and 1342 visual tris (colliders 312 and 148); each is one contiguous mesh (components 1, duplicate positions 0), and unlike portal.glb's the disc does not float -- it is fanned off the frame's own y=0 vertex row.
2. Pit: the lip at r 46.7 rounds over 1.0 m, then an earth-and-root bank drops to a dark floor at y -11.05 (r 42), no water. Fog: ForestFog, seven translucent discs y -12.5..-8 (alpha 0.5 at the floor, y -11.05, the old water height, easing to 0.06 three metres over it: the fog starts where the floor was and the bank stands clear down to it; zero inside r 6.8 round the trunk so no disc cuts it, zero 0.6 m into the bank so no edge shows; GL Compatibility has no fog volumes, so the fog is layers). KillBox roof at y 0.0, just over the thicket's tallest thorns, r 62, 30 m deep. The thicket: the floor is a grid of quads (rings r 42, 38 in to 11 by 1.5 at 120 columns, r 7 at 60, a centre cap under the trunk) and ~1140 barbed brambles out of single-quad floor sockets between r 11 and 38, roots 1.0 m apart, zone "cell" so they read as black spikes: a bramble is a three-sided tube whose every ring has one vertex pushed out 0.35..0.65 m, the side rotating ring to ring (a zigzag of thorns at six triangles a segment, rings every 1.1 m; no separate thorn geometry, which is what keeps 1140 plants inside the contract); 22 % are tall (6.5..8.5 m by the trunk down to 4..6 m at the bank, r 0.3..0.08, 60 % with a barbed side branch out of one stem quad), the rest scrub (4..6.5 m down to 2.5..4.5 m, r 0.2..0.08); all lean 0.3..0.7 of their height sideways and twist 20..90 deg as they climb so the crowns tangle, never inside r 8; highest tip y -2.8. Nothing on the bank, nothing over the rim. Rim and lane unchanged.
3. Tree tower (origin = Tower node, y 25.35; its own glb, one contiguous mesh): trunk from y -13 (r 8, 48 sides, 8 buttress roots on the pier bearings, cos(8a) fluting) tapering to r 5.2 at y 20 then flaring goblet-like to r 7.0 at the platform, y 27.05; a chamfered bark lip r 5.0..7.0 rising 0.55 m round the flat floor (r 5.0). 8 oval blade piers (r 0.9..0.6) grow out of the lip's top, lean out to r 8 and land in the canopy belly at y 33.0; one pointed arch between each pair (springing y 29.9, apex y 32.5 at r 8.3) = the guard's windows; one twig rail per opening at floor +0.95 (top y 28.2; the floor +0.45 rail would sit inside the lip), nothing else between y 28.2 and 29.5 (the guard's sightline to the lane passes r 7.5 at y 28.4). Guard eye y 28.7, 5.7 m over the lane, as map 1.
4. Tree roof: the canopy belly is the room's ceiling, y 33.0 at r 9 sagging to 32.5 at the centre, with 8 ribs (r 0.3) from the pier landings into a hanging keystone boss (a vault); the 8-lobed leaf disc above it, rim r ~11, top y 37.2; six short branches out of the top end in leaf clumps (tops y 39.7, under the ceiling at 40.25). The Watcher sits at Tower + 13.0 (y 38.35), nested in the crown under the leaf ceiling. 7728 tris, collider 332.
5. Leaf wall: from the lane's outer edge (r 57.3, outward-only bulges 0..0.5) up to the gallery roof at y 36.0, leaning out to r 60.0, 12 forest trunks grown from its columns as bark pilasters (protruding up to 1 m, root flare at the foot); collider is the flat wall r 57.3 (plus a prism per trunk) and the flat gallery roof y 36.0.
6. Roof (map 1's pattern, in leaves -- a gallery ceiling over the lane, the centre open to a much taller roof, and a walled cliff of cells between them). Gallery roof: dense leaf over the lane only, r 60 in to the pit lip at 46.7, y 36.0 (13 m over the grass), a 0.5 m sag mid-span and +-0.55 m lumps, lit leaf where it bulges and shade in the hollows; nothing hangs under it. Wall above the gallery: a leaf cliff off that rim, r 46.7 leaning out to 48.4, y 36.0 to 64.0, outward bulge 0..0.35. Canopy dome: from that head at y 64 over the ravine to y 77.4 at the pole (54 m over the lane, four times the gallery; the tree's crown, 37.2..39.7, stands up into it), rings r 44/40.5/37/33/28 at 120 columns then 22/14/7, underside billowed +-2.2 m. NO GAPS: the leaf is unbroken, every dome quad carries a face, nothing can read as a hole or a bright polygon from the lane or the tower. Five sun-wells instead, all on the sun's side (bearings 74..172): an organic blob of dome quads (a wobbly radius 2.4..4.2 m, sines at 2, 3 and 5 per turn, no two alike, the biggest connected island only) painted as lit "sun" leaf with two cells of lit leaf round it, its vertices pulled +-0.55 m in y and +-0.45 m in plan so the leaf hangs torn; 24 leaf clumps (r 0.9..1.7, stems 0.5..1.8 m) hung round them, one in three leaning 0.8..1.6 m out under the light, so each shaft's top is occluded from below.
7. Cells: two tiers in the leaf wall over the lane, six more in the wall above the gallery, all pointed-arch mouths with branch bars, dark inside. Lane wall (mouths 3.0 m wide, bars at 0.5 m pitch and one rung, 2.5 m deep): lower 12 (sill 0.3 m over the lane, jamb y 25.05, apex 26.8); upper 22, sill y 28.6, jamb 30.6, apex 31.8, two per bay between forest trunks staggered 6..7.5 deg off the lower cell, none between 328 and 12 deg (fence, portal), mouth centres >= 6 deg from a trunk. Above the gallery (mouths 2.44 m wide, bars at 0.8 m pitch, no rung, 2.0 m deep, recessed outward into the leaf over the roof): six tiers of 17, sills y 36.5 / 40.7 / 44.9 / 49.1 / 53.3 / 57.5, jambs 2.0 m over each sill, apexes 3.2 m over each sill (highest 60.7), one every 21.2 deg with every other tier half a bay off, like brick -- 102 mouths facing the tower across the ravine, then plain leaf to the dome's spring at y 64. 36 in the pit bank down its whole height, mouths 2.3 m wide x 3.6..5.4 m tall, visual only.
8. Ferns and hummocks grown out of the deck grid (welded sockets) along the wall foot (r 56.4..57.3) and the lip (r 46.7..47.6) only; nothing from r 47.6 to 56.4; ten hanging roots off the lip into the pit. Both meshes are one connected component each (PROVE in --check: components=1, duplicate_positions=0).
9. Bushes: forest_bush, a hand-placed cover prop for the lane, one script `tools/modelling/forest_bush_build.py` with two variants selected by `model build forest_bush --variant low|tall`. Low is 1.85 x 2.00 m across x 1.30 m tall, 752 tris drawn + 96 collider, and hides a crouched runner (1.2 m) from the tower but not a standing one; tall is 2.83 x 3.00 x 2.20 m, 1196 + 96, and hides a standing runner (1.8 m). Origin at the base centre, so it drops on the grass at identity. A nine-sided woody core with leaf clumps welded into sockets on its quads, one contiguous mesh, the shared forest atlas (zones shade/leaf/sun/bark, palette entry 11 unchanged). Collider is a `-colonly` eight-sided shell whose radius per height band and compass sector is measured off the drawn mesh at 0.86, not a box. PROVE for both: components=1, duplicate_positions=0, nonmanifold_edges=0, open_edges=0, slivers=0.
10. Sun rays: sun from bearing 120 deg at 52 deg elevation; one shaft per ceiling gap, three vanes crossed on the sun line, 1.8 m wide at the top tapering to 1.0 m for a 3 m gap (scaled to the gap), starting 0.9 m up inside its gap and running 0.6 m into what it lands on; tint and feather in the vertex colour; ForestRaysSolid (pale yellow, alpha 0.35, unshaded, mixed) and ForestRaysSoft (alpha 0.14, additive) share the geometry; the scene shows Soft under a DirectionalLight from the same side.
11. Palette (Ryan: "lean more towards Ocarina of Time and less toward Castle Crashers"): sampled from the refs (BotW Korok forest lit grass #99c03d, shaded #679030, ferns #63843f, shaded canopy #5a7c54; OoT Kokiri grass #696910, mist #92934b, trunks #75745e, deep foliage #353b24), muted a step darker so the shafts carry the light. Lane grass #8a9440 / #7a8a3c / #66803a, path #7c7448 / #6e6244, verge #86903e / #78823c / #767044, shaded edge #6c7c3c / #5c6e38 / #4e6034; leaf wall base #3a4a2c with leaves #5c7040 / #687a48 / #506638 lit #8a9856; ceiling underside base #26321f leaves #38482c / #30402a / #3c4e30 lit #4c5e3a; lit leaf tops #66783e with #869650 / #98a65c lit #b0b86c; fern #4a6236 fronds #6c8848 / #7c9652; bark #5e5440 / #584e3c / #645a46 cracks #322c20 moss #54683c; pit earth #4a3e2e / #44382a / #504432; roots and brambles #62503a; fog tint (0.62, 0.68, 0.54). Constants in forest_tree_build.py's palette block; USE_TEXTURE_FILES swaps in tools/modelling/textures/forest_atlas_albedo.png when present.
12. Budget: ground 110842 tris + rays 2 x 432 + fog 2016 = 113812 drawn (contract 116000; map 1 is 71k: the thicket costs 43k, 38 a plant), collider 920; tree 7728 (contract 12000), collider 332. PROVE: ground components=1 duplicate_positions=0 nonmanifold_edges=0 slivers=0; tree the same. Bot harness: 5/5 resolved on forest rules, 0 hazard deaths. Renders: forest_pit_fog.png from the lane looking down into the thicket, forest_pit.png from the tower standing on the lip in an arch (from the floor the lip hides the drop).
13. The wood (Ryan: "for the forest level, im not going to complete it, it just needs to look good, can you spread trees across the whole thing like in the beginning, it looks good"). "The beginning" is the beginning of the lap -- S1 The Stand, the trunk slalom at 14..74 deg from the obstacle-course pass -- and that is what the whole lap is now. The course is gone: the thorn hedges, the three boost pads, the speed orb and the standing stones fought the look and are removed; the pit, brambles, fog, rays, portal, bars, fence and hub wedge are untouched. 143 prop instances stand on the annulus, placed by `tools/forest_trees.py` (the layout is the data in its `LAYOUT` table, grown once from `SEED` 20260922 by `grow()`; `--check` proves the corridor, `--cover` predicts the bake, `--write` splices the block into `scenes/ring/forest.tscn` under `Sections/`). Nothing is welded into `forest.glb`.
    Four lines of trees run round the ring. **Lip** (r 47.45, 56 instances): in the ravine-edge fern band, off the walkable lane, `forest_tree_prop_a` and `_c` only (b's 1.28 m root flare would hang over the drop), the leaners spun to lean out across the path, with low bushes and boulders between. **Lane** (47): IN at r 49.9 or OUT at r 53.8 and never mid-lane -- the walkable lane is 7.25 m (r 48.25..55.50, the welded ferns carry collision) and one band plus the agent radius eats half of it -- swapping sides every one to three trees, with every swap at least 6.5 deg of run so the corridor snakes between the last tree on one side and the first on the other; the start pocket (329..12 deg) stays open. **Wall** (40): in the wall-foot fern band at r 56.55, off the walkable lane, standing between the leaf wall's own 12 trunks (3.5 deg clear) and its 12 lower cell mouths (1.9 deg clear), the leaners spun to lean in over the path. Nothing anywhere in 341..359 (portal 345, fence 350, bars 353). Trees are scaled 0.85..1.25 uniformly, a/b/c weighted about 4:4:3 on the lane; about a quarter of the slots on every line are undergrowth instead of a tree. Counts: 45 `_a`, 30 `_b`, 38 `_c`, 11 `forest_bush_low`, 4 `forest_bush_tall`, 14 `forest_rock_boulder`, 1 `forest_rock_outcrop`. The crown floor at 3.0 m clears the guard's line to a head on the lane (the ray from the eye at y 28.7 passes the lip at 2.05 m over the grass), so from the tower the wood is trunks and a ring of crowns, not a roof.
    PROVE, `tools/forest_trees.py --check`: 0 overlaps, narrowest corridor on the run 3.15 m (at 248.8 deg), 0 bearings pinched under 3.0 m, tightest side-swap diagonal 3.47 m. `tools/harness/check_course.gd --scene=res://scenes/ring/forest.tscn --lane-gaps`: `walk=ok`, 253 waypoints, 304.65 m (the course was 302.77, the empty lane 287.55); navmesh 1000 polygons, 1 lethal carved (the KillBox), 0 jump links, 0 pad links; `--lane-gaps` reports 5 blocked bearings, all in the pre-existing fence-and-bars band at 345..353, outside the run. Bot harness on `forest_match_rules.tres`: 10 matches, 10 resolved, 0 unresolved, 0 hazard deaths. Draw budget re-measured: 249815 tris, 152 surfaces, 16 materials, 2196 transparent, 2 lights, 1 shadow caster (the course measured 170443 / 54 / 20); `tests/test_map_draw_budgets.gd`'s forest row moved to tris 299778 (+95246 over the 204532 ceiling, i.e. the measurement plus 20 %) and surfaces 183 (from 65); materials, transparent, lights and shadow ceilings unchanged. Frame cost on the PC (`tools/perf/profile_match.gd`, RX 6600 XT, 1920x1080, 7 bots, 20 s each), before -> after: camera on a running bot 0.940 -> 1.134 ms mean, 3.704 -> 4.134 ms worst-1 %, 63.6 -> 136.0 draw calls, 286k -> 350k primitives; fixed at a runner's eye on the lane (pos 45.03,24.6,26.0 looking down the run) 0.986 -> 0.999 ms mean, 3.858 -> 3.489 ms worst-1 %, 46.4 -> 127.3 draw calls, 279k -> 339k primitives. As with the course, the instances are a draw-call cost and not a frame-budget one. Renders in ~/Desktop/panopticon-renders/forest/trees/: aerial (the cutaway from 0,95,55), lane (eye level down the run from 30 deg), guard (from the guard's eye at y 28.9), portal (down the last of the run to the finish).
13. Course (Ryan: "spin up a tree map, and marble map, to do its best job making an obstacle course for both of those with the elements it created. no rules, just let them try their best"). The lane was empty and the bake found ONE cover point on the whole map (docs/BOT_SWEEP.md): every occluder stood in the outer wall, and a shadow from the outer wall falls away from the lane. 45 prop instances now stand ON the annulus, placed by `tools/forest_course.py` (the layout is the data in its `COURSE` table; `--check` proves the corridor, `--cover` predicts the bake) and spliced into `scenes/ring/forest.tscn` under `Sections/`. Nothing is welded into `forest.glb`.
    Two measured facts shape every placement, both off `tools/harness/check_course.gd`. **The walkable lane is 7.25 m, not 10.6 m**: the ferns and hummocks welded at r 46.7..47.6 and r 56.4..57.3 carry collision, so the baked mesh runs r 48.25..55.50. One prop band plus the 0.5 m agent radius eats half of that, so a prop stands INSIDE the lane (r 50.0) or OUTSIDE it (r 53.7) and never in the middle; the corridor is then the other side, never under 3.15 m. **The bake samples a body off the navmesh, which recast floats 0.5 m over the grass** (`--sight` reports `navmesh_y=23.50` on a lane at y 23.0), so a "head" is 2.0 m up and nothing under about 2.6 m takes the guard's head line. The 2.20 m bush and the 2.11 m slab are therefore scaled 1.35x and 1.4x in Y -- map 1's own lever on `rock_wall` (scale y 0.43) -- and the 1.30 m boulder and low bush are left as what they are, cover for a crouched body and not a standing one.
    The guard's eye is on the ring's axis, so a shadow falls radially OUTWARD: a prop inside the lane shades the corridor outside it, a prop outside the lane shades nothing a runner can stand in. That is the whole grammar of the lap. A stretch that puts its props inside is a covered run; a stretch that puts them outside is a naked one; the sides swap, and the swap is an open crossing.
    Stretch by stretch, bearings along the run 5 -> 335: **start pocket 5-14**, open grass, one boulder outside. **S1 The Stand 14-74**, a trunk slalom -- four trunks inside the lane (18, 26, 34, 42) so the corridor runs outside them in shadow, an open crossing at 46-52, then three outside (54, 62, 70) so the corridor runs inside them in full view. **Pocket 78**, one standing stone inside. **S2 The Bramble Hedge 82-142**, `forest_thorns` strips laid ALONG the lane in 6 m runs that change sides twice (93 in, 108 out, 134 in), each under a TrapVolume; at 0.96 m they sit under the 1.11 m jump apex, so the 2 m width is a choice and not a wall, and the inside runs carry a thicket tall enough for head cover while the outside run carries none. **Pocket 146**. **S3 The Boulder Field 152-214**, three standing stones (156, 186, 210) about 30 deg -- 27 m -- of open ground apart are the only head cover; the boulders between them (164, 179, 194) hide a chest and not a head, and the two 0.70 m shelves are there to be jumped. **Pocket 218**. **S4 The Boost Run 222-282**, three `demon_pad` on the lane radius (224, 242, 260) aimed down the 16.25 deg chord that is their own 14.7 m throw, so a runner leaves at 18 m/s and 45 deg and lands at r 52.3 in the shadow of a thicket set just short of the landing (239, 257, 275). The chord's midpoint falls on a thorn patch's bearing, which is where the arc is at its 3.68 m apex and 0.78 m outboard of that patch's kill box, so the pad carries a runner clean past brambles the walker has to go round; walk it instead and the whole stretch is one outer corridor past the brambles with a speed orb parked outside the flight line at 234. **Pocket 282**. **S5 The Thicket 286-320**, single file: one thicket per bay with the sides alternating every 9 deg, so the lane snakes 3.7 m across itself four times and every second bay is the naked one. **S6 The Last Gap 320-335**, one bare shelf, one last stone at 332, then 11.8 m of nothing to the portal at 345.
    Counts: 7 trunks (3 `forest_tree_prop_b`, 2 `_a`, 2 `_c`), 9 `forest_bush_tall`, 4 `forest_bush_low`, 8 `forest_rock_slab`, 4 `forest_rock_boulder`, 3 `forest_rock_outcrop`, 3 `forest_thorns_strip`, 3 `forest_thorns_round`, 3 `demon_pad`, 1 speed orb = 45. Thorn patches are placed through `scenes/props/forest_thorns_{round,strip}.tscn`, which each show one of the .glb's two patches and carry that patch's TrapVolume (feet_only, grace 0.15 s, mask 1048577) lifted 0.15 m -- the lane's own +-0.12 m relief would otherwise let a runner stand on a bump above the kill plane.
    PROVE, all on `tools/harness/check_course.gd --scene=res://scenes/ring/forest.tscn`: cover points **1 -> 114** (map 1 is 192, map 2 is 164); navmesh 311 -> 651 polygons; jump links 0 -> 15 and pad links 0 -> 3, so every pad is live and the rocks are hoppable; 6 lethal volumes carved plus the KillBox. The lap is completable on foot -- `walk=ok`, 165 waypoints, 302.77 m against the empty lane's 287.55 m, so the weave costs 15 m. No continuous-cover lap: at 5 deg bins 44 of 72 bins hold no cover point at all, the longest run of consecutive covered bins is 20 deg of a 330 deg lap, and the longest open run is 30 deg. `--lane-gaps` reports 6 blocked bearings, all of them the pre-existing fence-and-bars band at 350-354, outside the run. Bot harness on `forest_match_rules.tres`: 10 matches, 10 resolved, 0 unresolved, 0 hazard deaths, clean-run rate 92.4 % (map 1 is 91.3 %). Draw budget re-measured: 170443 tris, 54 surfaces, 20 materials, 2196 transparent, 2 lights, 1 shadow caster; `tests/test_map_draw_budgets.gd`'s forest row moved to that plus 20 %. Frame cost on the PC (`tools/perf/profile_match.gd`, RX 6600 XT, 1920x1080): map alone 0.630 ms mean and 1.237 ms worst-1% at 82 draw calls and 322k primitives; in a bot match from the guard's eye 0.787 / 2.560 ms at 31-49 draw calls, where docs/PERF_REVIEW.md measured 15.6 before the props; worst case, a rim orbit with the whole arena in frame, 0.991 / 3.076 ms at 121 flat draw calls, 341k primitives and 35.9 MB VRAM. So the 45 instances roughly double the guard's draw calls and cost about a third of a millisecond: they are a draw-call cost, not a frame-budget one. Renders in ~/Desktop/panopticon-renders/forest/course/: lap_wide_000/090/180/270, four wide shots from over the ravine at y 52 that cover the lap between them; s1_stand, pocket, s2_hedge, s3_boulders, s4_boostrun, s5_thicket, s6_lastgap at eye level down each stretch; guard_eye from the guard's own eye at y 28.9, where the piers blank wedges of the lane and the props stand in what is left. lap_aerial, lap_aerial_low and lap_aerial_tight are true cutaways of the whole lap: the map mesh's normals face inward (signed volume is negative -- it is a room), so with default backface culling a camera OUTSIDE it sees through the closed canopy with no geometry change and no hidden nodes. lap_aerial_tight (pos 0,95,55, look 0,23,0) is the readable one -- y 95 clears the crown's y 77.4 and the lap fills most of the frame, with the trunks reading as green clumps on one arc and the stones as grey blocks on another; note the clumps ringing the centre are the great tree's own crown, not props. The two higher poses are washed out, because shot.gd hard-codes a 100 deg fov so framing is distance-only and the WorldEnvironment's fog at 0.006 eats most of the contrast over the 150 m that fits the ring in frame.

14. Brightness (Ryan: "the forest level is quite a bit too bright"). The map was lit like a room, not a wood: a bright sky feeding ambient through `ambient_light_source = 3`, a 1.2 sun, and a pale fog washing the far side flat. Every number is forest-only -- `scenes/ring/forest.tscn`'s own `SkyMat`/`Env`/`Sun` and the three unshaded overrides, plus `scenes/ring/forest_portal.tscn`'s `Glow` -- so no other map and no shared resource moves, and `GameSettings.brightness` (`tonemap_exposure`) is untouched. Sky: `sky_energy_multiplier` 1.3 -> 1.0, `sky_top_color` (0.66, 0.74, 0.62) -> (0.4, 0.45, 0.37), `sky_horizon_color` (0.8, 0.8, 0.56) -> (0.5, 0.52, 0.37), `ground_horizon_color` (0.4, 0.5, 0.28) -> (0.24, 0.3, 0.18), `ground_bottom_color` (0.16, 0.24, 0.12) -> (0.09, 0.14, 0.07). Environment: `ambient_light_energy` 0.7 -> 0.55, `ambient_light_color` (0.5, 0.56, 0.36) -> (0.3, 0.34, 0.23), `fog_light_energy` 0.9 -> 0.5, `fog_light_color` (0.56, 0.6, 0.42) -> (0.34, 0.38, 0.27); `fog_density` stays 0.006, because density is how deep the ravine reads and energy is how bright the veil is, and only the second was the complaint. Sun `light_energy` 1.2 -> 0.85. The three unshaded overrides multiply their vertex colour, so their albedo carries the shafts down a smaller step than the room: `RaySoftMat` and `RaySolidMat` (1,1,1) -> (0.7,0.7,0.7) and (0.75,0.75,0.75), `FogMat` -> (0.6,0.6,0.6) -- the shafts and the pit fog stay the brightest things in frame, which is the palette note at 11 ("muted a step darker so the shafts carry the light") finally paying out. Portal `Glow` `light_energy` 5.0 -> 3.0, still a beacon at the finish over a darker lane. The forest gets NO `TowerLight`: it never had one (map 1 and map 2 do), and the tree's own crown is the canopy, so adding a profile would have added light to a map asked to lose some. PROVE, `tools/shot.gd` at 1920x1080 on the PC, in `~/Desktop/panopticon-renders/forest/dark/`: mean linear luminance at the runner's eye on the lane (`--pos=45.03,24.6,26.0 --look=32.01,23.5,40.98`) 0.1066 -> 0.0315, and at the guard's eye (`--pos=0,28.9,0 --look=36.77,23.5,36.77`) 0.0414 -> 0.0090. The guard still reads the lane: the grass band, the boulders, the thorn strips and the bars are all legible through the arches, and a runner body is `shading_mode = 0` (unshaded, `scenes/bot/runner_body_material.tres`), so a runner's albedo reaches the screen untouched by any of this and gains contrast against the darker wood rather than losing it. Every guard-sight test is a raycast against `RingBake.STATIC_COLLIDER_MASK` and reads no light, environment or fog property, so none of them can move: `tools/test.sh` 475/475, hot paths and i18n clean, forest draw budget unchanged at 170133 tris / 2 lights / 1 shadow caster; bot harness on `forest_match_rules.tres` 10 matches, 10 resolved, 0 unresolved.

15. Fixes (Ryan: "on the forest, both gates are there and it should only be the better one, also, the collision object looks really complicated? is it? it can just be a big rectangle. also, the fog and thorns dont look good, they look like thorns coming out of a block of butter."). **Gates.** The two gates were the fallen-log fence at 350 deg -- a log sill, 19 sticks and a top rail welded into forest.glb, with its own collider box to y 34.5 -- and the `Bars` node at 353 (forest_bars.glb, the stand of saplings). The fence is gone: `_fence()`, the FENCE_* block, the fern exclusion round it and the collider box are removed from forest_build.py; the `Bars` node is the only barrier now (no scene node went; the fence was never a node). Ground still one mesh: components=1, duplicate_positions=0, nonmanifold_edges=0, slivers=0. **Collider.** forest_bars.glb's `ForestBarsCollision-colonly` was one box per strand segment (hundreds of boxes following 43 strands); it is ONE box now, 10.6 x 1.2 x 8.5 m (the stand's footprint by its height, 12 tris), still the `-colonly` node in the glb so every scene that instances the stand gets it. The gap proof is unchanged (widest 0.335 m, limit 0.38). No other forest prop carries a strand-following collider: the tree props collide as the trunk's own silhouette (on purpose: a runner behind the trunk is behind the collider), the bushes as an eight-sided barrel, the portal as a four-sided prism ring; none of them is changed. **Pit fog.** It was a ForestFog MeshInstance3D of seven flat 48-gon discs at y -12.5..-8.0 (0.75 m pitch), alpha 0.5 at the floor easing to 0.05 at the top with a 2.5 m radial fade, unshaded through FogMat: from the lane it read as one pale disc with the brambles planted on it. Same node, same override, new geometry (forest_pit_build.py's FOG block): fourteen 36-gon layers at y -11.6..4.0 (1.2 m pitch), alpha 0.18 at the floor falling as (1 - u/reach)^1.6 to exactly 0 at the top layer -- the fog ends in air, there is no top plane -- with a 9 m feather to the bank (alpha 0.42 mid-feather, 0 at 0.6 m inside the earth), a 6.8..13 m feather round the trunk, the tint 0.5x at the floor rising to 1.0 (gloom below, haze above, FOG_TINT (0.26, 0.28, 0.22)), and a low-frequency drift field (3/5/8 lobes round plus one radial term, near-vertical) that sets how high each column climbs (0.65..1.0 of the stack) and how thick it is (+-30 %), so the top of the fog rolls instead of lying flat. The brambles are untouched (tips y -2.8) and now stand in fog that is still 0.05..0.07 alpha a layer where their tops are, so they break through softly instead of standing on a lid. Fog tris 2016 -> 5040 (the forest's 130k contract has 278 spare); KillBox untouched, still lethal at y 0.
    What the butter actually was, measured in Godot on the PC with `tools/shot.gd` and material/environment probes (mean RGB of a patch of the disc from the lane pose, 0..255): the disc is the pit FLOOR -- painting the ground flat magenta turned it magenta -- a black-albedo (`cell` zone) plane 60..90 m from the eye that the Environment's depth fog flattens to one colour: (37, 41, 28) with everything on, (17, 19, 13) with the depth fog off, (5, 7, 4) with depth fog, sun and mesh fog all off. So half of it was the depth fog, the rest sun and sky on a black plane, and the mesh fog -- old or new -- contributed about 6/255 (fogzero probe 37 -> 37; nofog 37 -> 37), because its vertex tint is dark by design (0.08..0.16 linear through the 0.6 override) and a dark veil over a fog-flattened plane is invisible. A flat plane at one distance takes one fog colour, which is why it read as a block with a hard rim where the bank starts. The fix that reads is the Environment's own height fog: `fog_height = 8.0`, `fog_height_density = 0.1` on forest.tscn's `Env` (the only Environment change; `fog_density` 0.006 and every colour and energy from the brightness pass stay). Fog now thickens exponentially with depth below y 8 -- the floor and the bank foot grade into one mist, the bank thins upward to the rim, the lane at y 23 is untouched, there is no plane anywhere -- and the brambles stand in it as dark shapes fading downward, tops darkest. Probed at 0.05 / 0.1 / 0.2: 0.2 drowns the thicket (a lake of mist), 0.05 leaves the floor's rim readable; 0.1 is the one shipped (disc patch (55, 62, 43), bank foot (60, 65, 43): the same colour, no edge). The 14-layer ForestFog stays as the gloom under the mist: it is what darkens the thicket floor from the guard's eye (visible in after2 vs after); the fixed pit pose measured 1.112 ms mean before it and 0.890 after, so it is inside the noise.
