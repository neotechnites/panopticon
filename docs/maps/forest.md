# Map 3: the Forest

A clearing under a closed canopy. A ravine round a great tree; the tree is the tower.
World coordinates, Godot y up; bearings as map 1 (`pol(bearing, r, z)`).

1. Lane: grass annulus r 46.7..57.3 at y 23.0, gentle 2-D relief +-0.12 m (zero at both edges), painted as fine noise in three greens between gold-green (136,158,62) and cartoon green (98,152,66), a worn brown-green path r 50.5..53.5 with a verge one row either side, lane r 52, route 5 deg to 335 deg; a fallen log with a stick fence across the lane at 350 deg (collider box to y 34.5).
2. Pit: the lip at r 46.7 rounds over 1.0 m, then an earth-and-root bank drops to water at y -11.05 (map 1's sea height, 34 m) narrowing to r ~42; the water is a disc grown off the bank's waterline ring, its own tiling sheet; KillBox roof at y -10.55, r 62.
3. Tree tower (origin = Tower node, y 25.35; its own glb, one contiguous mesh like map 1's tower): trunk from y -13 (r 8, buttress-root lobes, 40 sides) to the split at y 14..18.5; 8 branch piers (r 1.4..0.9) grow out of the trunk through welded sockets, lean out to r ~8.5 and run up into the canopy underside; one pointed arch between each pair of piers (apex r 8.7, y 25.7), three twig rungs per opening; the openings show the far piers through.
4. Canopy platform: scalloped leaf disc r ~11, y 24.6..27.0; the guard's floor is flat, r 5.0 at y 27.05 (Tower + 1.70, as map 1), ringed by a 0.55 m leaf sill r 5.0..6.1; six thin branches grow out of the sill, lean in to y ~33 and end in leaf clumps (r 2.0), one side twig and clump (r 1.3) each: a partial roof, sky between, crown top y 35.9. Guard eye y 28.7, 5.7 m over the lane, as map 1.
5. Leaf wall: from the lane's outer edge (r 57.3, outward-only bulges 0..0.5) up to the ceiling at y 33.0, leaning out to r 60.0, 12 forest trunks grown from its columns; collider is the flat wall r 57.3 (plus a prism per trunk) and a flat ceiling y 33.0.
6. Leaf ceiling: a full dome over the ring, y 33 at the wall to y 41 over the tree, underside lumps +-0.45 m, 9 gaps ~3 x 3 m; bright sky only through the gaps. 12 forest trunks stand in the wall as bark pilasters grown from the wall grid (protruding up to 1 m, root flare at the foot, matching collider prisms).
7. Cells: 12 in the leaf wall (pointed-arch mouths 3.0 m wide x 3.5 m tall, sill 0.3 m over the lane, 2.5 m deep, dark inside) with branch bars welded into the sill and the arch at 0.42 m pitch and one rung; none between 330 and 10 deg. 36 in the pit bank down its whole height, mouths 2.3 m wide x 3.6..5.4 m tall as map 1's 2.5..5.5, visual only.
8. Ferns and hummocks grown out of the deck grid (welded sockets) along the wall foot (r 56.4..57.3) and the lip (r 46.7..47.6) only; nothing from r 47.6 to 56.4; ten hanging roots off the lip into the pit. Both meshes are one connected component each (PROVE in --check: components=1, duplicate_positions=0).
9. Sun rays: sun from bearing 120 deg at 52 deg elevation; one shaft per ceiling gap, three vanes crossed on the sun line, 1.8 m wide at the top tapering to 1.0 m at the foot, starting 0.9 m up inside its gap and running 0.6 m into the lane, the wall or the pit bank it lands on; tint and feather in the vertex colour (alpha peaks on the axis, zero along both edges and at both ends: no cap, no hard edge); ForestRaysSolid (pale yellow, alpha 0.35, unshaded, mixed: the mesh-only look) and ForestRaysSoft (alpha 0.14, additive) share the geometry; the scene shows Soft under a DirectionalLight from the same side, Solid hidden; forest_rays_compare.png renders both from one camera.
10. Budget: map 44402 tris (+324 per ray node), tree 6776 tris, both one component (PROVE); palette between green-gold and cartoon green: leaves (98,138,58) / (132,168,72) / (52,86,40), grass (108,146,60), bark (96,72,48), earth (86,64,44), water (44,96,104).
