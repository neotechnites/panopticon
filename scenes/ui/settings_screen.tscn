[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/ui/settings_screen.gd" id="1_settings_screen"]

[node name="SettingsScreen" type="Control"]
process_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
editor_description = "The settings screen on its own, for a main menu or a standalone test. PauseMenu does NOT load this scene -- it constructs a SettingsScreen directly -- so the two cannot drift apart at load time. Both talk to the same SettingsStore."
script = ExtResource("1_settings_screen")
